package com.yci.repository;

/**
 * Documentation holder. Each Spring Data repository lives in its own file
 * within this package (UserRepository, ChannelRepository, ...).
 */
public final class Repositories {
    private Repositories() {}
}
